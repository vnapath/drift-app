import { PlaceholderScreen } from '@/screens/PlaceholderScreen';

export function SendDriftScreen() {
  return (
    <PlaceholderScreen
      eyebrow="Text only"
      title="Send Drift"
      body="Compose a short anonymous message and release it into the pool."
    />
  );
}
